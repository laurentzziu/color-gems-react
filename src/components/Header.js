import React, { Component } from 'react';

const Header = (props) => {
  return (
    <header id="app-header" className="mt-4">
      <h2 className="text-center mb-0">Color Gems</h2>
      <h4 className="text-center mt-2">Experiment</h4>
    </header>
  )
}

export default Header;
